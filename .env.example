# OpenAI API Key for classification
# Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-replace-with-your-actual-key-here

# Database URL
# If running with Docker, this is handled automatically for the container.
# If running locally (npm run dev), use: postgresql://user:password@localhost:5432/aitweetfilter
DATABASE_URL=postgresql://postgres:password@localhost:5432/aitweetfilter
